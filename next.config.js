/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['i.imgur.com'], // Add Imgur domain here
    },
  }
  
  module.exports = nextConfig